// Local stand-ins for Widget/StatelessWidget, imported relatively by the
// style-rule fixture files below. All six rules are purely syntactic (no
// type resolution), so a local stub with the right name is enough to
// trigger them - keeping this whole fixture dependency-free.
class Widget {}

class StatelessWidget {}
